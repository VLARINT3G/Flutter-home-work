void main() {
  final number = 12347321;
  print(isPalindrome(number));
}

bool isPalindrome(int number) {
  final list = number.toString().split('');

  for (int i = 0; i < list.length ~/ 2; i++) {
    if (list[i] != list[list.length - i - 1]) {
      return false;
    }
  }

  return true;
}
